pipeline {
    agent { docker { image 'python:3.5.1' } }
 
    environment {
    }
    stages {
        stage('Template Stage') {
            steps {
                echo 'This is the template stage'
            }
        }
    }
}
